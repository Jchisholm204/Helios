# Training
To train the model, run the command `python src/train_script.py`.

The learning and logging parameters can be adjusted at the bottom of this file.

Before training, the captions must be encoded using the caption encoder script.
To run the caption encoder script, execute `python src/caption_encoder.py`
